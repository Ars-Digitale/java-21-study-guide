# Corso Java 21 – Versione Italiana

Questa cartella contiene la traduzione in italiano del corso **Java 21 Certification Course**.

La versione originale (in inglese) si trova in:

- `../en/`

> ⚠️ Stato: traduzione in corso.  
> Alcuni moduli potrebbero essere mancanti o parziali.

## Struttura prevista

- `module-00/` – Prerequisiti, setup ambiente, Eclipse
- `module-01/` – Basi del linguaggio
- `module-02/` – Strutture di controllo
- `module-03/` – API di base (String, Math, Date/Time…)
- `module-04/` – OOP, eccezioni, generics
- `module-05/` – Programmazione funzionale e Stream
- `module-06/` – Collezioni
- `module-07/` – Thread e concorrenza
- `module-08/` – File, I/O e NIO
- `module-09/` – Moduli (JPMS)
